// 3dModel.js

document.addEventListener("DOMContentLoaded", function() {
  // This code will run when the DOM is fully loaded
  
  // Get the canvas element
  const canvas = document.getElementById("mars");

  // Get the div element
  const earthBackgroundDiv = document.querySelector(".earthbackground");

  // Append the canvas to the div
  earthBackgroundDiv.appendChild(canvas);
});
